receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

processors:
  batch:

exporters:
  awsxray:
    region: "ap-northeast-1"

service:
  pipelines:
    traces:
      receivers:
        - otlp
      exporters:
        - awsxray
